project_id = "control-de-comportamiento"

[db]
major_version = 17

[db.seed]
# Local fixtures are applied only through `npm run db:seed:local`.
enabled = false
sql_paths = ["./seed/seed.sql"]

[auth]
site_url = "http://localhost:3000"
additional_redirect_urls = ["http://127.0.0.1:3000"]
